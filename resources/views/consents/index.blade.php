@extends('layouts.app')
@section('content')<div class="container py-4"><div class="d-flex justify-content-between mb-3"><div><h3>Consentimientos de Hemodiálisis</h3><p class="text-muted">Anexo 3 · historial inmutable por versión y fecha.</p></div>@can('consents.create')<a class="btn btn-primary align-self-center" href="{{ route('consents.create') }}">Nuevo consentimiento</a>@endcan</div><form class="card card-body mb-3"><div class="input-group"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Paciente o DNI"><button class="btn btn-outline-primary">Buscar</button></div></form><div class="card"><table class="table mb-0"><thead><tr><th>Paciente</th><th>Fecha</th><th>Versión</th><th>Decisión</th><th>Médico</th><th></th></tr></thead><tbody>@forelse($consents as $consent)<tr><td>{{ $consent->patient->full_name }}<br><small>{{ $consent->patient->dni }}</small></td><td>{{ $consent->consented_at->format('d/m/Y H:i') }}</td><td>{{ $consent->version }}</td><td>{{ $consent->accepted?'ACEPTA':'NO ACEPTA' }}</td><td>{{ $consent->physician?->name ?: '—' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('consents.show',$consent) }}">Ver</a></td></tr>@empty<tr><td colspan="6" class="text-center py-4">Sin consentimientos.</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $consents->links() }}</div></div>@endsection
