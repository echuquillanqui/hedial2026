@extends('layouts.app')
@section('content')<div class="container py-4"><div class="d-flex justify-content-between mb-3"><div><h3>Consentimientos de Hemodiálisis</h3><p class="text-muted">Se generan automáticamente con la primera atención mensual de cada paciente.</p></div>@can('consents.create')<a class="btn btn-outline-primary align-self-center" href="{{ route('consents.create') }}">Registro excepcional</a>@endcan</div><form id="consent-filters" class="card card-body mb-3"><div class="row g-2 align-items-end"><div class="col-md-3"><label for="consent-date" class="form-label fw-semibold">Fecha</label><input id="consent-date" type="date" class="form-control" name="date" value="{{ $date }}"></div><div class="col-md-7"><label for="consent-search" class="form-label fw-semibold">Paciente</label><input id="consent-search" class="form-control" name="search" value="{{ request('search') }}" placeholder="Paciente o DNI"></div><div class="col-md-2 d-grid"><button class="btn btn-outline-primary">Buscar</button></div></div></form><div class="card"><table class="table mb-0"><thead><tr><th>Paciente</th><th>Fecha</th><th>Versión</th><th>Decisión</th><th>Médico</th><th></th></tr></thead><tbody>@forelse($consents as $consent)<tr><td>{{ $consent->patient->full_name }}<br><small>{{ $consent->patient->dni }}</small></td><td>{{ $consent->consented_at->format('d/m/Y H:i') }}</td><td>{{ $consent->version }}</td><td>{{ $consent->accepted?'ACEPTA':'NO ACEPTA' }}</td><td>{{ $consent->physician?->name ?: '—' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('consents.show',$consent) }}">Ver</a></td></tr>@empty<tr><td colspan="6" class="text-center py-4">Sin consentimientos para la fecha seleccionada.</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $consents->links() }}</div></div>
<script>document.getElementById('consent-date').addEventListener('change', () => document.getElementById('consent-filters').requestSubmit());</script>
@endsection
