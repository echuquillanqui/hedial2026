@extends('layouts.app') @section('content')<div class="container py-4"><h2>Salud Mental</h2><div class="card shadow-sm border-0 mb-4"><div class="card-header bg-white fw-semibold">Órdenes pendientes</div><div class="list-group list-group-flush">@forelse($pendingOrders as $o)<div class="list-group-item d-flex justify-content-between"><span>{{ $o->patient->full_name }}</span><a class="btn btn-info btn-sm text-white" href="{{ route('psychology.create',$o) }}">Iniciar atención</a></div>@empty<div class="list-group-item text-muted">No hay órdenes pendientes.</div>@endforelse</div></div><div class="card shadow-sm border-0"><table class="table mb-0"><thead><tr><th>Fecha</th><th>Paciente</th><th>EQ-5D</th><th></th></tr></thead><tbody>@foreach($items as $i)<tr><td>{{ $i->assessment_date->format('d/m/Y') }}</td><td>{{ $i->order->patient->full_name }}</td><td>{{ $i->eq5dAssessment?->health_state ?? 'Pendiente' }}</td><td><a href="{{ route('psychology.show',$i) }}">Ver</a></td></tr>@endforeach</tbody></table></div>{{ $items->links() }}</div>@endsection
