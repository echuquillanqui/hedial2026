@extends('layouts.app') @section('content')<div class="container py-4"><h2>Servicio Social</h2><div class="card shadow-sm border-0 mb-4"><div class="card-header bg-white fw-semibold">Órdenes pendientes</div>@forelse($pendingOrders as $o)<div class="card-body border-bottom d-flex justify-content-between">{{ $o->patient->full_name }}<a class="btn btn-warning btn-sm" href="{{ route('social-work.create',$o) }}">Iniciar evaluación</a></div>@empty<div class="card-body text-muted">No hay órdenes pendientes.</div>@endforelse</div><table class="table"><thead><tr><th>Fecha</th><th>Paciente</th><th></th></tr></thead><tbody>@foreach($items as $i)<tr><td>{{ $i->assessment_date->format('d/m/Y') }}</td><td>{{ $i->order->patient->full_name }}</td><td><a href="{{ route('social-work.show',$i) }}">Ver</a></td></tr>@endforeach</tbody></table>{{ $items->links() }}</div>@endsection
