@extends('layouts.app') @section('content')<div class="container-fluid py-4"><div class="d-flex justify-content-between"><div><span class="badge bg-primary">ANEXOS 13-A/B/C</span><h2>Reportes mensuales de pacientes</h2></div><form><input type="month" name="month" value="{{ $month }}" class="form-control" onchange="this.form.submit()"></form></div><div class="row g-3 mt-2"><div class="col-lg-4"><div class="card shadow-sm border-0"><div class="card-header fw-bold">13-A · Pacientes nuevos <a target="_blank" href="{{ route('homologation.pdf',['annex'=>'13a','month'=>$month]) }}">PDF</a></div><div class="list-group list-group-flush">@forelse($new as $p)<div class="list-group-item">{{ $p->full_name }} <small>{{ $p->first_hd_date }}</small></div>@empty<div class="list-group-item text-muted">Sin registros</div>@endforelse</div></div></div>@foreach(['HOSPITALIZED'=>'13-B · Hospitalizados','DISCHARGED'=>'13-C · Egresos'] as $type=>$label)<div class="col-lg-4"><div class="card shadow-sm border-0"><div class="card-header fw-bold">{{ $label }} <a target="_blank" href="{{ route('homologation.pdf',['annex'=>$type==='HOSPITALIZED'?'13b':'13c','month'=>$month]) }}">PDF</a></div><div class="list-group list-group-flush">@forelse($events->where('type',$type) as $e)<div class="list-group-item">{{ $e->patient->full_name }}<br><small>{{ $e->occurred_at->format('d/m/Y') }} · {{ $e->reason }}</small></div>@empty<div class="list-group-item text-muted">Sin registros</div>@endforelse</div></div></div>@endforeach</div><div class="card mt-4 shadow-sm border-0"><div class="card-header fw-bold">Registrar hospitalización o egreso</div><div class="card-body"><form method="POST" id="eventForm" class="row g-2">@csrf<div class="col-md-4"><select class="form-select" onchange="eventForm.action='{{ url('homologacion/pacientes') }}/'+this.value+'/evento'" required><option value="">Paciente…</option>@foreach(\App\Models\Patient::where('sede_id',\App\Support\CurrentSede::id())->orderBy('surname')->get() as $p)<option value="{{ $p->id }}">{{ $p->full_name }}</option>@endforeach</select></div><div class="col-md-2"><select name="type" class="form-select"><option value="HOSPITALIZED">Hospitalización</option><option value="DISCHARGED">Egreso</option></select></div><div class="col-md-2"><input type="datetime-local" name="occurred_at" class="form-control" required></div><div class="col-md-2"><input name="reason" class="form-control" placeholder="Motivo" required></div><div class="col-md-2"><button class="btn btn-primary">Registrar</button></div></form></div></div></div>@endsection
