@extends('layouts.app') @section('content')<div class="container py-4"><h2>Anexo 17 · Investigación epidemiológica viral</h2><div class="card card-body shadow-sm"><form method="POST" action="{{ route('homologation.epidemiology.store') }}" class="row g-3">@csrf<div class="col-md-6"><select name="patient_id" class="form-select" required><option>Paciente…</option>@foreach($patients as $p)<option value="{{ $p->id }}">{{ $p->full_name }}</option>@endforeach</select></div><div class="col-md-3"><input type="date" name="investigated_at" class="form-control" required></div><div class="col-md-3"><input name="suspected_infection" class="form-control" placeholder="VIH, VHB, VHC, HTLV…" required></div>@foreach(['clinical_manifestations'=>'Manifestaciones clínicas','dialysis_history'=>'Antecedentes de diálisis','risk_factors'=>'Factores de riesgo','contacts'=>'Contactos','control_measures'=>'Medidas de control','classification'=>'Clasificación','observations'=>'Observaciones'] as $f=>$l)<div class="col-md-6"><textarea name="{{ $f }}" class="form-control" placeholder="{{ $l }}"></textarea></div>@endforeach<div><button class="btn btn-danger">Registrar ficha y vincular serologías</button></div></form></div><table class="table mt-4"><tr><th>Fecha</th><th>Paciente</th><th>Sospecha</th></tr>@foreach($items as $i)<tr><td>{{ $i->investigated_at->format('d/m/Y') }}</td><td>{{ $i->patient->full_name }}</td><td>{{ $i->suspected_infection }}</td></tr>@endforeach</table>{{ $items->links() }}</div>@endsection
